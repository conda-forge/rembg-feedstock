{% set name = "rembg" %}
{% set version = "2.0.73" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/rembg-{{ version }}.tar.gz
  sha256: f6c2718d0ea0edaabcd11b272f54de64b0a44ef3c526a3af75d7dafd5e67efd9
  patches:
    - cli.patch

build:
  entry_points:
    - rembg=rembg.cli:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python {{ python_min }}
    - setuptools >=65.5.1
    - wheel
  run:
    - jsonschema
    - numpy
    - onnxruntime
    - opencv
    - pillow
    - pooch
    - pymatting
    - python >={{ python_min }},<3.12
    - scikit-image        # Not imported
    - scipy
    - tqdm
    # Optional for CLI extras
    - aiohttp
    - asyncer
    - click
    - fastapi
    - filetype
    - gradio
    - python-multipart
    - starlette           # Imported but not in requirements
    - uvicorn
    - watchdog

test:
  imports:
    - cv2
    - rembg
  commands:
    - pip list
    # - pip check  # Fails due to opencv
    - rembg --help  # Added patch to fix this
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://pypi.org/project/rembg
  summary: Remove image background
  license: MIT
  license_file: LICENSE.txt
  dev_url: https://github.com/danielgatis/rembg

extra:
  recipe-maintainers:
    - danielgatis
    - goanpeca
